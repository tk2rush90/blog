import { StrokeButtonDirective } from './stroke-button.directive';

describe('StrokeButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new StrokeButtonDirective();
    expect(directive).toBeTruthy();
  });
});
