import { AutoScrollItemDirective } from './auto-scroll-item.directive';

describe('AutoScrollItemDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoScrollItemDirective();
    expect(directive).toBeTruthy();
  });
});
