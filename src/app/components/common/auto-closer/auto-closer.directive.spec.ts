import { AutoCloserDirective } from './auto-closer.directive';

describe('AutoCloserDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoCloserDirective();
    expect(directive).toBeTruthy();
  });
});
