import { InlineButtonDirective } from './inline-button.directive';

describe('InlineButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new InlineButtonDirective();
    expect(directive).toBeTruthy();
  });
});
