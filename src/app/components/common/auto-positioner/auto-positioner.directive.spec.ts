import { AutoPositionerDirective } from './auto-positioner.directive';

describe('AutoPositionerDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoPositionerDirective();
    expect(directive).toBeTruthy();
  });
});
